import Main from "./components/main";
function App({ pageConfig }) {
    return (
        <div className="App">
            <Main pageConfig={pageConfig} />
        </div>
    );
}

export default App;
